ports:
  - name: iosel
    direct: output
    width: 1
    pos: [120, -40]
  - name: out
    direct: output
    width: 1
    pos: [120, 0]
  - name: in
    direct: input
    width: 1
    pos: [120, 40]
params: []
size: [240, 120]
textX: -50
software:
  className: Digital
  memSize: 0x10
  member:
    - note: // Select Input (Mode::IN) or Output (Mode::OUT)
      def: void mode(Mode mode);
      use: ${INSTANCE}.mode(Digital::Mode::);
    - note: // Read value (Mode::IN)
      def: uint32_t read();
      use: = ${INSTANCE}.read();
    - note: // Write value (Mode::OUT)
      def: void write(uint32_t val);
      use: ${INSTANCE}.write();
