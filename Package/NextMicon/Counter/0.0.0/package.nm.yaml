ports:
  - name: of
    direct: output
    width: 1
    pos: [120, 0]
params: []
size: [240, 60]
textX: -40
software:
  className: Prescaler
  memSize: 0x10
  member:
    - note: "// Set Counter"
      def: "void set(uint32_t);"
      use: "${INSTANCE}.set();"
    - note: "// Set Counter by Frequency"
      def: "void freq(uint32_t);"
      use: "${INSTANCE}.freq();"
