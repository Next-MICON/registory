ports:
  - name: tx
    direct: output
    width: 1
    pos: [120, -20]
  - name: rx
    direct: input
    width: 1
    pos: [120, 20]
params: []
size: [240, 80]
textX: -50
software:
  className: Serial
  memSize: 0x10
  member:
    - doc: "print(char)"
      copy: "${INSTANCE}.print()"
    - doc: "print(const char*)"
      copy: "${INSTANCE}.print()"
    - doc: "print(uint32_t)"
      copy: "${INSTANCE}.print()"
    - doc: "read() -> char"
      copy: "${INSTANCE}.read()"
